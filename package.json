{
  "name": "across-the-board",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node tests/engine.test.mjs && node tests/server.test.mjs && node tests/expansion.test.mjs && node tests/match-effects.test.mjs",
    "db:generate": "drizzle-kit generate",
    "deploy:self": "npm run build && wrangler d1 migrations apply DB --remote --config wrangler.selfhost.json && wrangler deploy --config wrangler.selfhost.json",
    "setup:self": "node scripts/configure-selfhost.mjs"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.4",
    "drizzle-orm": "0.44.5",
    "esbuild": "^0.28.2",
    "wrangler": "^4.130.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
